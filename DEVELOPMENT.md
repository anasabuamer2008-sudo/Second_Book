# سجل التطوير | Development Log

هذا الملف يلخّص كل خطوة قمت بها عند بناء **Second Book | الكتاب الثاني | הספר השני**، مع ما تم إنجازه في كل مرحلة.

This file summarizes every step taken while building **Second Book**, with what was achieved in each step.

---

## الخطوة 1: تهيئة المشروع | Project Setup

- إنشاء مشروع **Next.js 16 (App Router)** مع TypeScript و Tailwind CSS v4 عبر `create-next-app`.
- تثبيت `zustand` (إدارة الحالة) و `uuid` (أرقام الطلبات) و `resend` (البريد الاختياري).

---

## الخطوة 2: البنية والترجمة المزدوجة (i18n) | Structure & i18n

- بناء المسار الديناميكي `[lang]` لدعم **العربية `ar`** و **العبرية `he`** (الاثنتان RTL).
- ملفات ترجمة مركزية: `src/dictionaries/ar.json` و `he.json` مع دالة `getDictionary()` آمنة (ترجع العربية افتراضياً لأي لغة غير معروفة).
- توجيه عبر `src/proxy.ts` (convention الجديد بدل middleware):
  - `/` → ينتقل تلقائياً حسب Accept-Language.
  - مسار غير معروف (`/fr/...`) → ينتقل للافتراضي.
- صفحات SSG كاملة: `/ar` و `/he` وكل الأقسام — تحميل فوري بدون خادم.

---

## الخطوة 3: إدارة الحالة (Zustand) | State Management

- `src/store/useCartStore.ts`: سلة + طلبات محفوظة في `localStorage` (persist).
- أنواع TypeScript كاملة (`Book`, `Order`, `CartStore`) جاهزة للربط بقاعدة بيانات مستقبلاً.
- منع الإضافة المكررة لنفس الكتاب (نسخة واحدة).

---

## الخطوة 4: المكونات الأساسية | Base Components

- `Navbar`: شعار + مبدّل لغة ثنائي (العربية/עברית مع تمييز المختار) + أيقونة سلة بعداد + تنقل سفلي للموبايل.
- `Footer`: الحقوق لـ **أنس أبو عامر** و **عبدالله زيد**، المطوّر **@AbdullahZaid-ggg**، رابط Instagram فقط، ومبدّل لغة.
- `LanguageSwitcher`: Segmented control يعرض اللغتين مع تمييز المختارة + Prefetch فوري للتبديل السريع.
- `CartIcon`, `Toast`, `NotFoundClient` (صفحة 404 مخصصة).

---

## الخطوة 5: التصميم والخطوط (نظام المكتبة) | Design & Typography

- لوحة ألوان "مكتبة" دافئة: بنّي جلدي `#6b4226` / ذهبي `#c2933c` / كريمي ورقى `#f6f0e4`.
- الخطوط عبر `next/font` (محلية، مجزأة، سريعة):
  - `h1` والعناوين الرئيسية: **Amiri** (عربي) / **Frank Ruhl Libre** (عبري) — خطوط الكتب.
  - `h2`, `h3` والجسم: **Cairo** (عربي) / **Heebo** (عبري).
- هرمية واضحة حيث يختلف خط كل مستوى عنوان.
- حجم خط 16px+ و line-height 1.75 لراحة القراءة بالعربية والعبرية.

---

## الخطوة 6: الكتب بتصميم 3D خفيف | Lightweight 3D Books

- `Book3D`: غلاف كتاب ثلاثي الأبعاد **بـ CSS فقط** (Perspective + rotateY + حافة الصفحات) — بدون مكتبات 3D أو صور ثقيلة.
- `BookCard`: غلاف 3D محتوى داخل مساحته (لا يغطي المعلومات)، ثم المعلومات: العنوان، المؤلف، **شارة الحالة**، **شارة "نسخة واحدة فقط"**، الوصف، السعر وزر الإضافة.
- تأثير Hover رفع النغمة + ظهور ناعم (`fade-up`).

---

## الخطوة 7: الصفحة الرئيسية | Home Page

- قسم البطل: العنوان "Second Book" + الاسم باللغتين (العربية والعبرية) + وصف + زر تصفح.
- شريط الثقة: 3 نقاط (كتب مراجعة بعناية / نسخة واحدة فقط / أسعار منصفة).
- **بحث فوري** في العناوين والمؤلفين + **تصفية حسب الحالة**.
- شبكة الكتب + Toast عند الإضافة.
- **JSON-LD Schema** (BookStore) لتحسين محركات البحث.
- صورة الشعار/الأيقونة: من حساب Instagram `@second_book07`.

---

## الخطوة 8: السلة والطلبات | Cart, Checkout & Orders

- الصفحة الرئيسية ← إضافة للسلة ← `cart` (إزالة/مجموع) ← `checkout`.
- رقم طلب فريد بصيغة `BK-YYYYMMDD-XXXX`.
- حفظ الطلب محلياً + إرسال إيميل منسق (اختياري عبر Resend).
- صفحة نجاح + **زر مشاركة عبر واتساب**.
- صفحة `orders`: سجل الطلبات السابقة من الجهاز.
- حالات فارغة مصممة جيداً (Empty States).

---

## الخطوة 9: البريد الإلكتروني | Email (optional, frontend-first)

- `src/components/email/orderEmail.ts`: قالب HTML مضمّن (جداول متجاوبة RTL) بلغة العميل.
- `src/app/api/send-order/route.ts`: تحقق خادمي + إرسال عبر Resend **فقط إذا وُجدت المفاتيح** — بدونها يعمل الموقع بواجهة أمامية خالصة.
- `.env.example` مع `RESEND_API_KEY` و `ORDER_EMAIL`.

---

## الخطوة 10: الأداء والأمان | Performance & Security

- `next/font` محلي — لا استدعاءات CDN.
- كل الصفحات **Static SSG** — تحميل فوري.
- بحث وتصفية داخل المتصفح (لا طلبات خادم).
- أرقام طلبات UUID عشوائية.
- رؤوس أمان في `next.config.ts` (X-Frame-Options, nosniff, Referrer-Policy).
- `sitemap.ts` للغتين + Open Graph + hreflang في `generateMetadata`.
- SVG مضمّنة بدون مكتبات أيقونات؛ صورة واحدة فقط (الشعار) بحجم ~175KB.
- تخلصنا من تحذير "middleware" بالهجرة إلى `proxy.ts`.

---

## الخطوة 11: الإزالة والضبط حسب الطلب | Scope Adjustments

- إزالة صفحة "بيع كتاب" نهائياً — الموقع **للشراء فقط**؛ البيع حصري من المالك.
- إبقاء اللغتين العربية والعبرية فقط.
- تعديل العنوان: "Second Book" في الـ h1 مع عرض الاسم بالنسختين (الكتاب الثاني | הספר الثاني).
- مبدّل لغة يعرض اللغتين مع تمييز المختار.
- الحقوق: **أنس أبو عامر** و **عبدالله زيد** | المطوّر: **@AbdullahZaid-ggg**.

---

## الخطوة 12: التوثيق والتحقق | Docs & Build Check ✅

- `README.md` شامل + هذا الملف (سجل التطوير).
- `npm run build` ناجح بلا أخطاء ولا تحذيرات:
  - 12 صفحة SSG (ar + he) + API + sitemap.
- الموقع خفيف: لا حمولات ضخمة، يستجيب بسرعة، وجاهز للتوسع نحو Backend.

---

## 🏁 الخلاصة | Conclusion

تم بناء متجر **Second Book** كاملاً: واجهة أمامية خفيفة وسريعة بالعربية والعبرية (RTL)، تصميم "مكتبة" دافئ بخطوط كتب أصلية، كتب بتأثير 3D خفيف بـ CSS فقط، سلة وطلبات محلية، بحث وتصفية فورية، تحسينات SEO، وأمان أساسي — كل ذلك **بدون خادم يعتمد عليه المستخدم**، مع جاهزية كاملة لإضافة Backend (قاعدة بيانات / بريد فعلي) لاحقاً بدون إعادة بناء الواجهة.